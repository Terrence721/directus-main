# 📝 TODO

**Last Updated:** September 4, 2026 (packages/stores: useServerStore + useAppStore added; audited against the real source and against itself)

A phase-by-phase log of what's been done on this repo and what's still open. This is the source of truth for
progress — a GitHub Project board (once set up) will be a lighter-weight view of the same work, kept in sync with
this file, not a separate source of truth.

**What this repo is:** a personal demonstration workspace, not a fork or continuation of the real Directus
project. It's built one file at a time, taking the real,
battle-tested Directus project as a reference point rather than a source to reproduce — redesigning is the default,
not the exception. MIT-licensed. Node.js, Vue 3, and Yarn throughout.

## At a glance

**Done, in full:**

| Item                | Detail                                                                                                                                  | Phase   |
| ------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| Repo bootstrap      | Root `package.json`, `.yarnrc.yml` catalog, and `.gitignore` all in place. `yarn install` resolves clean, no errors or warnings         | Phase 1 |
| CI                  | GitHub Actions `CI` (format/lint/lint:style/build/test) and `CodeQL` workflows added, both verified locally before commit               | Phase 2 |
| Labels & milestone  | `code-review` label added (repo's default set + this one, matching `platform-main`'s label set); `Repo infrastructure` milestone opened | Phase 2 |
| Project board       | `directus-main` board (user project #6) brought in line with `platform-main` (project #2), field-for-field                              | Phase 2 |
| Root file scaffold  | `LICENSE` (MIT), `README.md`, `.gitattributes` (LF normalization), `.editorconfig` all added                                            | Phase 2 |
| Scrum board content | 5 issues closed into `Done`, 1 issue (`Add packages/stores`, #6) opened, matching `platform-main`'s issue-per-item pattern              | Phase 2 |

**In progress right now:** `packages/stores` — tracked as [issue #6](https://github.com/Terrence721/directus-main/issues/6). `useAuthStore`, `useUserStore`, `useServerStore`, and `useAppStore` are all done and tested, 100% coverage. The real source's `packages/stores` only has one store (`useAppStore`, 7 flat refs) — ours covers the same ground plus more, split across 4 purpose-specific stores instead of one grab-bag. See the **Still to do** table below for what's left before `app/` (the actual Vue application) can start consuming these.

## ✅ Done

### Repository bootstrap

| Phase | Commit    | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----- | --------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | `904ea51` | 2026-09-04 | Root `package.json` added: `directus-monorepo`, `private: true`, Yarn workspace glob (6 patterns), `packageManager: yarn@4.18.0` (verified current against Yarn Berry's own GitHub releases, not the stale `yarn` npm package), `engines.node: 26`. No `scripts`/`devDependencies` yet — every real root script needed either a tool not installed yet or a `catalog:` reference to a file that didn't exist yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 1     | `232084d` | 2026-09-04 | `todo.md` ledger added, structured after this author's `platform-main` repo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 1     | `341a25c` | 2026-09-04 | `.yarnrc.yml` catalog added — shared dependency versions for the whole workspace, each checked against real npm latest at time of writing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 1     | `439da97` | 2026-09-04 | Root `.gitignore` added (`node_modules/`, `dist/`, the local Yarn install-state, and machine-local files `cspell.json`/`*.code-workspace`). `package.json` `scripts`/`devDependencies` filled in against the catalog. First real `yarn install` run, and every issue it surfaced fixed: `typescript` pinned to `6.0.3` instead of the just-released `7.0.2`, since `typescript-eslint@8.69.0` (current latest) declares a peer range of `typescript >=4.8.4 <6.1.0` and doesn't support the new TS 7 major yet; three undeclared peer dependencies added (`postcss`, `stylelint-config-html`, `stylelint-config-recommended`) that the Vue/HTML stylelint configs need but don't list as direct deps; `@changesets/cli`, `eslint`, and `stylelint` each fell back one version because the literal latest was still inside npm's publish quarantine window at write time. `yarn.lock` committed |

### CI

| Phase | Commit | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----- | ------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2     | —      | 2026-09-04 | `.github/workflows/ci.yml` added (`actions/checkout@v7`, `actions/setup-node@v7`, Node 26, Corepack, `yarn install --immutable`, then `format`/`lint`/`lint:style`/`build`/`test`) and `.github/workflows/codeql.yml` added (`github/codeql-action@v4`, `javascript-typescript`, weekly schedule plus push/PR). Needed real lint/format config to back the existing root scripts, so also added: `eslint.config.js` (flat config — `@eslint/js`, `typescript-eslint`, `eslint-plugin-vue` flat/recommended, `eslint-plugin-import-x`, `eslint-config-prettier` last), `.prettierrc.json`/`.prettierignore` (tabs, single quotes, matching this repo's existing style), `.stylelintrc.json` (standard + SCSS + Vue configs, `postcss-html` for `.vue`/`.html`, `stylelint-use-logical`). Added `--allow-empty-input` to `lint:style` since no `.vue`/`.css`/`.scss` files exist yet. All five scripts run clean locally (0 errors) before being trusted in CI. Both workflows confirmed passing against the pushed commit, not just locally |

### Labels & milestone

| Phase | Commit | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ----- | ------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2     | —      | 2026-09-04 | `code-review` label added on top of GitHub's default set, matching `platform-main`'s label list exactly (the repo's pre-existing `accessibility` label was kept, since it's genuinely relevant to a Vue UI project). `Repo infrastructure` milestone opened for CI/tooling/board work not scoped to one package — per-package milestones deferred until real packages exist, following the `platform-main` pattern of one milestone per module once that module is being worked |

### Project board

| Phase | Commit | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----- | ------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2     | —      | 2026-09-04 | `directus-main` user project (#6) — already existed empty from earlier this session, since GitHub Projects v2 are user-scoped and survived the repo deletion — brought exactly in line with `platform-main` (#2): `Status` single-select field's five options fixed to the same names and colors (Backlog/gray, Planned/blue, In Progress/yellow, Verification & QA/orange, Done/green), the default view renamed `View 1` → `Board`, its `BOARD_LAYOUT` grouped by `Status` (matches `platform-main`, so items render as columns), and the project linked to the `Terrence721/directus-main` repository (it wasn't linked to any repo before) |

### Root file scaffold

| Phase | Commit    | Date       | What                                                                                                                                                                                                                                                                                                          |
| ----- | --------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2     | `2910112` | 2026-09-04 | `LICENSE` added, plain MIT with only this author's copyright — checked the real Directus project's actual license first (Monospace Sustainable Core License, source-available, not permissive) but MIT was chosen since this repo isn't reproducing Directus source verbatim, per the redesign-first approach |
| 2     | `c109534` | 2026-09-04 | `README.md` added: non-affiliation notice, links to `todo.md` and the Project board, AI-assisted-development disclosure, structured after `platform-main`'s README but with its own framing (redesign-first, not fidelity-first)                                                                              |
| 2     | `33fd191` | 2026-09-04 | `.gitattributes` added (`* text=auto eol=lf`) — normalizes line endings to LF regardless of local git config, fixing the repeated CRLF/LF warnings seen on every commit so far. Confirmed it doesn't retroactively flag already-committed files as modified                                                   |
| 2     | `24d8007` | 2026-09-04 | `.editorconfig` added — tabs by default (matching this repo's actual `.prettierrc.json`, not blindly copied from `platform-main`'s space-based one), spaces for `*.yml` (YAML doesn't allow tabs), unlimited line length for `*.md`                                                                           |

### Scrum board content

| Phase | Commit | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----- | ------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2     | —      | 2026-09-04 | Board (#6) was structurally correct but had 0 items — matched `platform-main`'s pattern of real, closed GitHub Issues per completed unit of work rather than draft items: opened issues #1–5 (`Repo bootstrap`, `CI/CD: CI + CodeQL GitHub Actions workflows`, `Labels & milestone setup`, `GitHub Project board`, `Root file scaffolding`), each labeled `enhancement`, milestoned `Repo infrastructure`, closed, added to the board, and set to `Done`. Opened a new `Module additions` milestone (mirroring `platform-main`'s) and issue #6 (originally `Real source, package by package`), left open, added to the board, and set to `Backlog` |

### packages/stores

| Phase | Commit                          | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----- | ------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2     | `ff5f1b5`, `cb1e1fd`, `2023fe1` | 2026-09-04 | Package scaffolding: `vue`/`pinia`/`@vue/devtools-api` added to the catalog; `package.json` (`@directus/stores`, `pinia` dependency, `vue` peer dependency at `^3.5.11` matching pinia's own peer range — `catalog:` isn't valid inside `peerDependencies`, Yarn auto-rewrote it to `"*"`, fixed by hand); `tsconfig.json` (ES2022, Bundler resolution, strict). Two real CI-breaking mistakes made and fixed here: pushing `yarn.lock` before the `package.json` that requests those resolutions exist (`--immutable` correctly refused to silently rewrite it), and referencing a `tsconfig.json`/`src/` that weren't committed yet — the lesson going forward is to hold pushes until the whole working tree is internally consistent, verified locally, then push once so CI evaluates the final state rather than a broken intermediate commit                                        |
| 2     | `7bea0a2`                       | 2026-09-04 | `useAuthStore` added (`src/auth.ts`) — session `accessToken`/`expiresAt`, `loggedIn` computed, `setSession`/`clearSession`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2     | `dcecf12`                       | 2026-09-04 | `eslint-import-resolver-typescript` added and wired into `eslint.config.js` — `eslint-plugin-import-x`'s resolver couldn't resolve relative imports written as `./auth.js` pointing at real `auth.ts` files (the correct convention for ESM `.ts` source under `moduleResolution: Bundler`, since it also works unmodified under plain Node ESM)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2     | `73e4030`                       | 2026-09-04 | Real bug fixed in `useAuthStore`: `loggedIn` only checked `accessToken !== null`, never `expiresAt`, so an expired session still reported as logged in. Also fixed a structural risk alongside it — `accessToken`/`expiresAt` were two independently-nullable refs that had to be kept in lockstep by hand; consolidated into one `session: Session \| null` ref so that can't desync                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2     | `c2013ea`, `8bd0cf6`, `5b3f31e` | 2026-09-04 | `vitest`/`vite` added to the catalog (vitest's `vite` peer is non-optional) and to `packages/stores`; `test` script added; `tsconfig.json` updated to exclude `src/**/*.test.ts` from the build so test files don't leak into `dist/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2     | `2fcc2be`                       | 2026-09-04 | Tests added for `useAuthStore` (4 cases, including a regression test for the expiry bug above)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2     | `c322f44`                       | 2026-09-04 | `forceConsistentCasingInFileNames` enabled in `tsconfig.json` — real gap, since local dev is on Windows (case-insensitive filesystem) but CI runs on Ubuntu (case-sensitive); a wrong-case import would've passed locally and only broken in CI                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2     | `8530f8a`                       | 2026-09-04 | `useUserStore` added (`src/user.ts`) — `CurrentUser` shape, `fullName`/`initials` computed (with real edge-case handling: falls back to email when no name is set)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2     | `977047e`                       | 2026-09-04 | Tests added for `useUserStore` (5 cases)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2     | `87bc914`, `3bb4d40`            | 2026-09-04 | `@vitest/coverage-v8` added to the catalog and to `packages/stores`, plus a `test:coverage` script — real gap, the root `test:coverage` script existed but nothing actually provided coverage, so it (and the Vitest VS Code extension's coverage run) failed. Both stores now at 100% statement/branch/function/line coverage                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2     | `2815172`                       | 2026-09-04 | `useUserStore` and `CurrentUser` exported from `src/index.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2     | `8504c00`, `8c5cec3`            | 2026-09-04 | Editor tooling gaps fixed: `.vscode/extensions.json` now recommends Vitest/ESLint/Prettier/Stylelint/Volar and marks Angular Language Service unwanted (this is a Vue project, not Angular). Added `.vscode/settings.json` (committed) with `typescript.tsdk` and formatter settings — previously these only existed in the local, gitignored `.code-workspace` file, so anyone else opening the plain folder got none of them                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| —     | —                               | 2026-09-04 | Issue #6 rescoped from the generic "Real source, package by package" to "Add packages/stores" and moved from `Backlog` to `In Progress` on the board, matching `platform-main`'s pattern of specific per-package issues rather than one broad catch-all                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2     | `37802cf`                       | 2026-09-04 | `useServerStore` added (`src/server.ts`) — `ServerInfo`/`AuthProvider` shape (project name/branding, auth providers, registration flag), `hasCustomBranding` and `ssoProviders` (filters out the `local` driver) computed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| —     | —                               | 2026-09-04 | Read-only, one-time authorized look at `F:\directus-main\directus-main\packages\stores\src` (the real source) to answer a direct question about the file-count difference — confirmed the real package has just 2 files (`index.ts`, `app.ts`) and one store, `useAppStore`: 7 flat, independent refs (`notificationsDrawerOpen`, `hydrated`, `hydrating`, `error`, `authenticated`, `accessTokenExpiry`, `basemap`), no computed properties, no actions, and even a `@TODO maybe move to userStore` comment acknowledging the auth-adjacent fields don't really belong there. The standing "never touch F: drive" rule resumed immediately after this single authorized read                                                                                                                                                                                                              |
| 2     | `a381c27`, `0cdaf43`, `ec2205c` | 2026-09-04 | Completeness audit of every file in `packages/stores` at that point, prompted by the file-count question above: `Session` interface wasn't `export`ed from `auth.ts` (inconsistent with `CurrentUser`/`ServerInfo`, so consumers couldn't import it), fixed and re-exported from `index.ts`; `package.json`'s strict `exports` map blocked `@directus/stores/package.json` resolution (needed by some tooling that reads a package's version directly), added the standard self-export                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2     | `513dd21`, `895c449`            | 2026-09-04 | Real test gaps closed: `auth.test.ts` and `user.test.ts` only asserted derived values (`loggedIn`, `fullName`) and never the actual stored `session`/`user` objects — 100% line/branch coverage doesn't catch a bug like swapped fields, since every line still executes either way. Added `toEqual` assertions on the raw stored state in both                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2     | `e9738ae`                       | 2026-09-04 | Tests added for `useServerStore` (5 cases, including exact-array assertions on `ssoProviders`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2     | `8b2855f`                       | 2026-09-04 | `useAppStore` added (`src/app.ts`) — closes the gap found above relative to the real source's app-shell state (hydration lifecycle, notifications drawer). Deliberately redesigned rather than matching the real source's 3 independent refs: `hydrated`/`hydrating`/`error` are collapsed into one discriminated `HydrationStatus` union (`idle` \| `hydrating` \| `hydrated` \| `error`) so the app can't end up in an invalid combination (e.g. `hydrated` and `error` both set) the way 3 separately-mutated refs could — the same class of bug fixed in `useAuthStore` above. `notificationsDrawerOpen` stayed a plain independent boolean, since it's unrelated to hydration and coupling it in would've been wrong. Left out `basemap` (no map feature exists in this repo) and the real source's duplicate `authenticated`/`accessTokenExpiry` (already covered by `useAuthStore`) |
| 2     | `87981d9`                       | 2026-09-04 | Tests added for `useAppStore` (6 cases covering the full hydration state machine and the drawer toggle, including its optional explicit-value argument)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2     | `ce0e754`                       | 2026-09-04 | `useAppStore` and `HydrationStatus` exported from `src/index.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

## 🚧 Still to do

| Item                            | Detail                                                                                                                                                                                        |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/` workspace                | The actual Vue application — `index.html`, `main.ts` (bootstraps `createApp` + `createPinia`), `App.vue`, `vite.config.ts` — is what turns these stores into a real, visible app. Not started |
| API client                      | Nothing currently calls `auth.setSession(...)` outside of tests — a real login flow needs something to actually talk to a backend. Not started                                                |
| Routing                         | `vue-router`, for `/login` and whatever comes after. Not started                                                                                                                              |
| Real source, remaining packages | Once `packages/stores` is reasonably complete: Vue-ecosystem packages prioritized, then whatever else has the widest fan-in                                                                   |

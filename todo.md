# 📝 TODO

**Last Updated:** September 5, 2026 (diagrams/ added — 4 real architecture diagrams, guided by conduit-full's set)

A phase-by-phase log of what's been done on this repo and what's still open. This is the source of truth for
progress — a GitHub Project board (once set up) will be a lighter-weight view of the same work, kept in sync with
this file, not a separate source of truth.

**What this repo is:** a personal demonstration workspace, not a fork or continuation of the real Directus
project. It's built one file at a time, taking the real,
battle-tested Directus project as a reference point rather than a source to reproduce — redesigning is the default,
not the exception. MIT-licensed. Node.js, Vue 3, and Yarn throughout.

**Target scope:** the full real `packages/` list (~28 entries), plus `app/`, `api/`, and `sdk/` — not just the
Vue-frontend-relevant subset. Built one real, dependency-ordered package at a time.

**On the drift sweep:** a full audit of this repo (code, docs, GitHub issues/board, wiki, external portfolio
surfaces) found several places that had gone stale relative to actual progress — most visibly `README.md` and
`portfolio.html` still describing a Phase 2 snapshot (e.g. "48 commits," "1 package shipped") against a Phase 3
reality of 130+ commits and 3 shipped packages plus a deployed app. **All of it is now fixed**: the real bugs found
along the way (a genuine test-isolation issue, an uncovered router guard branch, two stale `--passWithNoTests`
flags), `README.md`/`portfolio.html`, GitHub issues #7/#8 (closed and corrected) plus a retroactive #9 for
`packages/errors`, the wiki (`Constants`/`Errors`/`App` pages added), and both external surfaces (profile README,
portfolio hub card). Nothing is mid-flight right now — see **Still to do** for the genuine remaining work, each
already tracked as its own backlog issue (#11–#13).

## At a glance

**Done, in full:**

| Item               | Detail                                                                                                                                                                                                                                                                         | Phase   |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| Repo bootstrap     | Root `package.json`, `.yarnrc.yml` catalog, and `.gitignore` all in place. `yarn install` resolves clean, no errors or warnings                                                                                                                                                | Phase 1 |
| CI                 | GitHub Actions `CI` (format/build/lint/lint:style/test, with retry logic on install) and `CodeQL`, both verified locally before commit                                                                                                                                         | Phase 2 |
| Labels & milestone | `code-review` label added (repo's default set + this one, matching `platform-main`'s label set); `Repo infrastructure` milestone opened                                                                                                                                        | Phase 2 |
| Project board      | `directus-main` board (user project #6) brought in line with `platform-main` (project #2), field-for-field                                                                                                                                                                     | Phase 2 |
| Root file scaffold | `LICENSE` (MIT), `README.md`, `.gitattributes` (LF normalization), `.editorconfig` all added                                                                                                                                                                                   | Phase 2 |
| `packages/stores`  | 4 stores (`useAuthStore`, `useUserStore`, `useServerStore`, `useAppStore`), 20 tests, 100% coverage, audited against the real source and against itself                                                                                                                        | Phase 2 |
| Portfolio surfaces | `portfolio.html` + GitHub Pages deploy, portfolio hub card, profile README entry, and wiki (`Home`, `Stores`, `Constants`, `Errors`, `App`) all live and current as of the drift sweep                                                                                         | Phase 2 |
| `app/` workspace   | Vite + Vue 3 + Pinia; a real, styled, routed app (`vue-router`, `/login` and `/` with an auth guard) wired to a real (simulated) `authClient` and a working logout button; live at [terrence721.github.io/directus-main/app](https://terrence721.github.io/directus-main/app/) | Phase 3 |
| `diagrams/`        | 4 real architecture diagrams (system architecture, auth flow, data model, testing strategy), structure guided by `conduit-full`'s own diagram set, restyled in this repo's paper/ink palette; live and linked from `README.md`                                                 | Phase 3 |

**In progress right now:** nothing — the drift sweep is fully closed out. See the **Still to do** table below for the real backlog (`#11` session persistence, `#12` `api/` workspace, `#13` remaining packages).

## ✅ Done

### Repository bootstrap

| Phase | Commit    | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----- | --------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | `904ea51` | 2026-09-04 | Root `package.json` added: `directus-monorepo`, `private: true`, Yarn workspace glob (6 patterns), `packageManager: yarn@4.18.0` (verified current against Yarn Berry's own GitHub releases, not the stale `yarn` npm package), `engines.node: 26`. No `scripts`/`devDependencies` yet — every real root script needed either a tool not installed yet or a `catalog:` reference to a file that didn't exist yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 1     | `232084d` | 2026-09-04 | `todo.md` ledger added, structured after this author's `platform-main` repo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 1     | `341a25c` | 2026-09-04 | `.yarnrc.yml` catalog added — shared dependency versions for the whole workspace, each checked against real npm latest at time of writing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 1     | `439da97` | 2026-09-04 | Root `.gitignore` added (`node_modules/`, `dist/`, the local Yarn install-state, and machine-local files `cspell.json`/`*.code-workspace`). `package.json` `scripts`/`devDependencies` filled in against the catalog. First real `yarn install` run, and every issue it surfaced fixed: `typescript` pinned to `6.0.3` instead of the just-released `7.0.2`, since `typescript-eslint@8.69.0` (current latest) declares a peer range of `typescript >=4.8.4 <6.1.0` and doesn't support the new TS 7 major yet; three undeclared peer dependencies added (`postcss`, `stylelint-config-html`, `stylelint-config-recommended`) that the Vue/HTML stylelint configs need but don't list as direct deps; `@changesets/cli`, `eslint`, and `stylelint` each fell back one version because the literal latest was still inside npm's publish quarantine window at write time. `yarn.lock` committed |

### CI

| Phase | Commit    | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2     | —         | 2026-09-04 | `.github/workflows/ci.yml` added (`actions/checkout@v7`, `actions/setup-node@v7`, Node 26, Corepack, `yarn install --immutable`, then `format`/`lint`/`lint:style`/`build`/`test`) and `.github/workflows/codeql.yml` added (`github/codeql-action@v4`, `javascript-typescript`, weekly schedule plus push/PR)                                                                                                                                                    |
| 2     | `7e27ed7` | 2026-09-04 | Real CI ordering bug found and fixed: `packages/stores` importing from `@directus/constants` failed to resolve in `Lint` on a fresh checkout, because `ci.yml` ran `Lint`/`Test` _before_ `Build` — `@directus/constants`'s `package.json` points at `dist/index.js`, which doesn't exist until `Build` runs. Fixed by moving `Build` right after `Format`. Verified by deleting all `dist/` output and re-running the full suite in the new order before pushing |
| 3     | `497ba59` | 2026-09-05 | Retry logic added to the `Install dependencies` step (3 attempts, backoff) after hitting a real transient Yarn/Node `write ECANCELED ... SSL destruction` failure — confirmed transient by re-running the exact same job (it passed), matching a known bug class already documented once before in this project's history                                                                                                                                         |

### Labels & milestone

| Phase | Commit | Date       | What                                                                                                                                                                                                |
| ----- | ------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2     | —      | 2026-09-04 | `code-review` label added on top of GitHub's default set, matching `platform-main`'s label list exactly. `Repo infrastructure` milestone opened for CI/tooling/board work not scoped to one package |

### Project board

| Phase | Commit | Date       | What                                                                                                                                                                                                                                                                                |
| ----- | ------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2     | —      | 2026-09-04 | `directus-main` user project (#6) brought exactly in line with `platform-main` (#2): `Status` field's five options matched (names + colors), the default view renamed `View 1` → `Board`, grouped by `Status`, and the project linked to the `Terrence721/directus-main` repository |

### Root file scaffold

| Phase | Commit    | Date       | What                                                                                                                                                                                                                                                         |
| ----- | --------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2     | `2910112` | 2026-09-04 | `LICENSE` added, plain MIT with only this author's copyright — checked the real Directus project's actual license first (Monospace Sustainable Core License, source-available) but MIT was chosen since this repo isn't reproducing Directus source verbatim |
| 2     | `c109534` | 2026-09-04 | `README.md` added: non-affiliation notice, links to `todo.md` and the Project board, AI-assisted-development disclosure                                                                                                                                      |
| 2     | `33fd191` | 2026-09-04 | `.gitattributes` added (`* text=auto eol=lf`) — normalizes line endings to LF regardless of local git config                                                                                                                                                 |
| 2     | `24d8007` | 2026-09-04 | `.editorconfig` added — tabs by default (matching `.prettierrc.json`), spaces for `*.yml` (YAML doesn't allow tabs)                                                                                                                                          |

### Scrum board content

| Phase | Commit | Date       | What                                                                                                                                                                                                                                                                                                                                                     |
| ----- | ------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2     | —      | 2026-09-04 | Opened issues #1–5 (`Repo bootstrap`, `CI/CD`, `Labels & milestone setup`, `GitHub Project board`, `Root file scaffolding`), each labeled `enhancement`, milestoned `Repo infrastructure`, closed, set to `Done`. Opened `Module additions` milestone and issue #6, set to `Backlog`, later rescoped to "Add packages/stores" and moved to `In Progress` |
| 3     | —      | 2026-09-05 | Issue #6 closed (`packages/stores` genuinely complete), moved to `Done`. Opened issue #7 (`Add packages/constants`), added to the board, set to `In Progress`                                                                                                                                                                                            |
| 3     | —      | 2026-09-05 | Opened issue #8 (`Add app/ workspace`), added to the board, set to `In Progress`                                                                                                                                                                                                                                                                         |

### packages/stores

| Phase | Commit                          | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----- | ------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2     | `ff5f1b5`..`2023fe1`            | 2026-09-04 | Package scaffolding: `vue`/`pinia`/`@vue/devtools-api` added to the catalog; `package.json`, `tsconfig.json`. Two real CI-breaking mistakes made and fixed: pushing `yarn.lock` before the `package.json` that needs it, and referencing a `tsconfig.json`/`src/` not yet committed — lesson: hold pushes until the whole tree is internally consistent, push once                                                                                                                                                                                                                                                                                                                                                                            |
| 2     | `7bea0a2`                       | 2026-09-04 | `useAuthStore` added — session `accessToken`/`expiresAt`, `loggedIn` computed, `setSession`/`clearSession`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2     | `dcecf12`                       | 2026-09-04 | `eslint-import-resolver-typescript` added — `eslint-plugin-import-x` couldn't resolve `./auth.js` imports pointing at real `auth.ts` files (the correct ESM convention under `moduleResolution: Bundler`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2     | `73e4030`                       | 2026-09-04 | Real bug fixed: `loggedIn` never checked `expiresAt`, so an expired session still reported logged in. Also consolidated `accessToken`/`expiresAt` (two independently-nullable refs) into one `session: Session \| null` ref so they can't desync                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2     | `c2013ea`..`5b3f31e`            | 2026-09-04 | `vitest`/`vite` added to the catalog and to `packages/stores`; `test` script added; `tsconfig.json` excludes `src/**/*.test.ts` from the build                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2     | `2fcc2be`                       | 2026-09-04 | Tests added for `useAuthStore` (4 cases, including a regression test for the expiry bug)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2     | `c322f44`                       | 2026-09-04 | `forceConsistentCasingInFileNames` enabled — real gap, since local dev is Windows (case-insensitive) but CI runs Ubuntu (case-sensitive)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2     | `8530f8a`                       | 2026-09-04 | `useUserStore` added — `CurrentUser` shape, `fullName`/`initials` computed with real edge-case handling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2     | `977047e`                       | 2026-09-04 | Tests added for `useUserStore` (5 cases)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2     | `87bc914`, `3bb4d40`            | 2026-09-04 | `@vitest/coverage-v8` added plus a `test:coverage` script — root script existed but nothing provided coverage. Both stores now at 100% coverage                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2     | `2815172`                       | 2026-09-04 | `useUserStore`/`CurrentUser` exported from `src/index.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2     | `8504c00`, `8c5cec3`            | 2026-09-04 | Editor tooling gaps fixed: `.vscode/extensions.json` recommends Vitest/ESLint/Prettier/Stylelint/Volar, marks Angular Language Service unwanted. Committed `.vscode/settings.json` (`typescript.tsdk`, formatters) — previously only in the local, gitignored `.code-workspace`                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2     | `37802cf`                       | 2026-09-04 | `useServerStore` added — `ServerInfo`/`AuthProvider` shape, `hasCustomBranding` and `ssoProviders` computed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| —     | —                               | 2026-09-04 | Read-only, one-time authorized look at the real `packages/stores/src` to answer a file-count question — confirmed it's 2 files, one store (`useAppStore`, 7 flat independent refs, no computed/actions, with a `@TODO maybe move to userStore` comment on the auth-adjacent field). Standing "never touch F: drive" rule resumed immediately after                                                                                                                                                                                                                                                                                                                                                                                            |
| 2     | `a381c27`, `0cdaf43`, `ec2205c` | 2026-09-04 | Completeness audit: `Session` wasn't exported from `auth.ts` (inconsistent with other stores' types), fixed; `package.json`'s strict `exports` map blocked `@directus/stores/package.json` resolution, added the standard self-export                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2     | `513dd21`, `895c449`            | 2026-09-04 | Real test gaps closed: `auth.test.ts`/`user.test.ts` only asserted derived values, never the raw stored state — 100% coverage doesn't catch swapped fields. Added `toEqual` assertions on the actual stored objects                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2     | `e9738ae`                       | 2026-09-04 | Tests added for `useServerStore` (5 cases, exact-array assertions on `ssoProviders`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2     | `8b2855f`                       | 2026-09-04 | `useAppStore` added — closes the gap vs. the real source's app-shell state. Redesigned rather than copied: `hydrated`/`hydrating`/`error` collapsed into one discriminated `HydrationStatus` union so an invalid combination can't be represented, the same fix class as `useAuthStore`'s. `notificationsDrawerOpen` stayed a plain boolean (unrelated concern). Left out `basemap` (no map feature exists) and the duplicate `authenticated`/`accessTokenExpiry` (already covered by `useAuthStore`)                                                                                                                                                                                                                                         |
| 2     | `87981d9`                       | 2026-09-04 | Tests added for `useAppStore` (6 cases covering the hydration state machine and the drawer toggle)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2     | `ce0e754`                       | 2026-09-04 | `useAppStore`/`HydrationStatus` exported from `src/index.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2     | `fe38010`, `2283a1a`            | 2026-09-04 | `@directus/constants` added as a real dependency (`workspace:*`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2     | `7497cdc`                       | 2026-09-04 | `useServerStore`'s `ssoProviders` now compares against `LOCAL_AUTH_DRIVER` instead of the hardcoded `'local'` string — a real cross-package consumer for the new constants package, not a speculative one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2     | `399364f`                       | 2026-09-04 | Explicit gaps/bugs + composition-over-inheritance + DRY + SOLID audit: confirmed zero `class`/`extends` anywhere in the codebase (composition is already the only pattern in use); found and fixed a real DRY violation — `useUserStore`'s `fullName`/`initials` each independently re-filtered the same name fields. Extracted a plain `getNameParts()` helper (not a second `computed()`, since that first attempt added a redundant, never-taken null-guard and dropped coverage from 100% to 98% before being corrected)                                                                                                                                                                                                                  |
| 3     | `b37652a`                       | 2026-09-05 | `vitest.config.ts` added — explicit `environment: 'node'` (no DOM needed for pure Pinia stores) and coverage config, part of fixing the VS Code Vitest extension not discovering tests in this multi-package workspace                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| —     | —                               | 2026-09-05 | Real-source re-check (one-time authorized F: drive read): confirmed `@directus/stores` genuinely has only `useAppStore` — `useAuthStore`/`useUserStore`/`useServerStore` here are a deliberate scope expansion, not a reproduction. The real equivalents of `useUserStore`/`useServerStore` actually live one level up, in the real app's own `app/src/stores/user.ts`/`server.ts` (app-specific state, not shared-package state); real auth isn't a Pinia store at all — `app/src/auth.ts` is a plain 176-line module using `universal-cookie` for real token persistence, directly relevant to backlog [#11](https://github.com/Terrence721/directus-main/issues/11). See the **Full real-source inventory** below for the complete picture |

### Portfolio surfaces

| Phase | Commit    | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----- | --------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2     | `994c7cb` | 2026-09-04 | `README.md`: portfolio link at the top, new title (`Principal Frontend Engineering Demonstration (Vue/Node.js/Yarn)` — replacing "Directus, Redesigned," which read too much like an official rebrand), Wiki link, refreshed "What's Here So Far"                                                                                                                                                                                                                                                                  |
| 2     | `2bb4fd9` | 2026-09-04 | `portfolio.html` added — same visual system as `platform-main`'s portfolio page (paper/ink theme, light/dark), but every number is this repo's own real, current, and smaller-scale state, not copied stats. Includes a ledger-style breakdown of the 4 completeness-audit findings and the "judgment calls" story about building more than the real source's flat single store                                                                                                                                    |
| 2     | `cb0b09d` | 2026-09-04 | `.github/workflows/pages.yml` added (`actions/upload-pages-artifact@v5`, `actions/deploy-pages@v5` — verified current majors, not copied from `platform-main`'s older pinned versions) — minimal version that just serves `portfolio.html`, since this repo doesn't have `platform-main`'s test-report/diagram infrastructure yet. GitHub Pages enabled via the API, repo `description` and `homepage` updated to match the redesign-first framing (the old description read like generic Directus marketing copy) |
| —     | —         | 2026-09-04 | `terrence721.github.io` portfolio hub: added a `directus-main` project card (early-stage `status-strip` format, matching `eShop-full`/`AxonFramework-Full`) and a footer repo link. `Terrence721/Terrence721` profile README: added a `directus-main` featured-project entry matching the existing format exactly. Both had been explicitly removed after the original repo's deletion; both readded now that there's real, current content to describe                                                            |
| —     | —         | 2026-09-04 | `directus-main` wiki: `Home.md` and `Stores.md` pushed. Real friction along the way — a GitHub wiki's git repo doesn't exist until the first page is saved through the web UI at least once (no API/git-only way to create it); once created, the placeholder `Home` page had to be merged with `--allow-unrelated-histories -X ours` to keep the real content                                                                                                                                                     |
| 3     | `c0d1ab0` | 2026-09-05 | `pages.yml` updated to actually build the workspace (Node/Corepack/install/`yarn build`, mirroring `ci.yml`'s proven steps) and publish `app/dist` to `site/app`, alongside `portfolio.html` at `site/portfolio.html` — the deploy job previously only ever copied the static portfolio page, since `app/` didn't exist yet when it was written                                                                                                                                                                    |

### packages/constants

| Phase | Commit               | Date       | What                                                                                                                                                                                                                                                                                                                                                                   |
| ----- | -------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2     | `345e074`..`780af66` | 2026-09-04 | Package scaffolding (`package.json`, `tsconfig.json`) plus `PERMISSION_ACTIONS`/`PermissionAction`, chosen as the first constant since it's core RBAC vocabulary with zero external dependencies — real dependency graph checked first (`constants` has zero deps in the real source; `types` isn't even a pure leaf, it depends on `@directus/ai`/`@directus/schema`) |
| 2     | `cdddca4`..`aa8aa87` | 2026-09-04 | `LOCAL_AUTH_DRIVER` added — the first constant with a real, existing consumer rather than a speculative one (see `packages/stores` above)                                                                                                                                                                                                                              |

### app/ workspace

| Phase | Commit               | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----- | -------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3     | `940a9e0`, `8ca879c` | 2026-09-05 | Catalog additions checked against the real `app/package.json`'s actual `devDependencies` (one-time authorized F: drive read): `@vitejs/plugin-vue`, `vue-tsc`, `@vue/test-utils`, `happy-dom` (not `jsdom` — the real app uses `happy-dom`, an informed choice worth following), plus `@vue/compiler-dom` and `@vue/devtools-api` once `yarn install` surfaced them as real, non-optional peer requirements                                                                                                                                         |
| 3     | `a7ef4ed`, `51f5bf6` | 2026-09-05 | `app/package.json` added — informed by the real app's structure (everything in `devDependencies` since an app is a leaf, never installed by anything else; a separate `typecheck` script via `vue-tsc --noEmit` since Vite's build doesn't type-check) but scoped to only what's real here: `@directus/stores` as the one workspace dependency, nothing fabricated for packages that don't exist yet (`composables`, `types`, `utils`, etc., all real deps of the actual app)                                                                       |
| 3     | `48bc0ce`            | 2026-09-05 | `app/index.html` added — Vite's entry point                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 3     | `dfe9480`            | 2026-09-05 | `app/src/main.ts` added — `createApp` + `createPinia()`, mounts to `#app`                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 3     | `0bb03ba`            | 2026-09-05 | `app/src/App.vue` added — a real, working root component (not a placeholder): consumes `useAppStore`'s hydration lifecycle and `useAuthStore`'s `loggedIn` to actually render one of 4 states                                                                                                                                                                                                                                                                                                                                                       |
| 3     | `24ca571`            | 2026-09-05 | `app/vite.config.ts` added (`@vitejs/plugin-vue`) — build succeeds                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 3     | `2b37a77`            | 2026-09-05 | `app/tsconfig.json` added (`noEmit: true` — unlike the library packages, Vite/esbuild does the actual bundling, `vue-tsc` is purely for type-checking) — fixes the `typecheck` script, which had been dumping CLI help text with no config to run against                                                                                                                                                                                                                                                                                           |
| 3     | `8bc7924`            | 2026-09-05 | Real gap found and fixed: `vitest run` exits code 1 when there are no test files yet (same class of issue already fixed for `stylelint` via `--allow-empty-input`) — added `--passWithNoTests` to `app`'s `test`/`test:coverage` scripts (later removed once the workspace had real tests — see the drift-sweep rows below)                                                                                                                                                                                                                         |
| —     | —                    | 2026-09-05 | Actually verified in a real browser, not just a clean build: started the dev server, installed Playwright + a Chromium binary (no project skill or `chromium-cli` existed for this yet), navigated to the page, and confirmed it renders exactly `"Please log in."` with zero console errors — screenshot inspected directly, matching this session's own standing rule to verify UI changes in a browser rather than trusting the build alone                                                                                                      |
| 3     | `5c826e7`            | 2026-09-05 | Root `vitest.config.ts` added, using Vitest's `projects` field (`packages/*`, `app`) — fixes the VS Code Vitest extension not discovering any tests across this multi-package Yarn workspace, since it was relying on package.json script-sniffing alone                                                                                                                                                                                                                                                                                            |
| —     | —                    | 2026-09-05 | A stray, machine-specific `"vitest.nodeExecutable": "C:\\nvm4w\\nodejs\\node.exe"` (plus duplicate `files.exclude`/`search.exclude`) ended up in the committed, shared `.vscode/settings.json` during the same troubleshooting — reverted from there and moved into the local, gitignored `.code-workspace` file instead, since a hardcoded personal Node path would break for any other contributor                                                                                                                                                |
| 3     | `d8903a0`, `7a0c9aa` | 2026-09-05 | `@directus/errors` added as a real dependency to `app` — `LoginForm.vue` needs `isDirectusError` to tell a real API error apart from a generic failure                                                                                                                                                                                                                                                                                                                                                                                              |
| 3     | `92de8f2`            | 2026-09-05 | `app/vitest.config.ts` added — `environment: 'happy-dom'` plus `@vitejs/plugin-vue` registered separately (Vitest's own transform pipeline doesn't read `vite.config.ts`), needed for `app`'s first component test                                                                                                                                                                                                                                                                                                                                  |
| 3     | `87011a3`            | 2026-09-05 | `LoginForm.vue` added — the first real UI component, not a placeholder. Designed with dependency inversion: takes an `onSubmit` prop (a function) instead of knowing how login actually works, so it's fully testable with no real API/backend. Real bug found and fixed: disabling the submit button doesn't block an Enter-key submit fired from another field, so a second submit could still start while the first was in flight — fixed with an early-return guard on `submitting`                                                             |
| 3     | `c089567`            | 2026-09-05 | Tests added for `LoginForm` (5 cases): submits the entered credentials, shows a loading state with the button disabled and reverts to "Sign in" after resolving, shows a `DirectusError`'s own message on failure, falls back to a generic message for a non-`DirectusError`, ignores a concurrent submit                                                                                                                                                                                                                                           |
| 3     | `955c971`            | 2026-09-05 | Tests added for `App.vue` (4 cases), closing a gap that had existed since `App.vue` was written — it had only ever been checked by eye in a real browser, never covered by an automated test. One branch (`hydrating === true`) is disclosed as genuinely untestable right now: `onMounted` calls `startHydration()` then `completeHydration()` synchronously, with no real async gap yet to observe in between                                                                                                                                     |
| 3     | `b0b6769`            | 2026-09-05 | `App.vue` now renders `LoginForm` instead of plain "Please log in." text when logged out, via a `handleLogin` stub (commented as a placeholder pending a real API client) that calls `auth.setSession(...)` directly and accepts any credentials                                                                                                                                                                                                                                                                                                    |
| 3     | `ac6ba77`            | 2026-09-05 | `App.test.ts` updated for the wiring above: the old exact-text assertion on "Please log in." was replaced with a check that the login form renders, plus a new end-to-end case that submits real credentials through the form and asserts the app reaches "Welcome back."                                                                                                                                                                                                                                                                           |
| 3     | `9f7a57f`            | 2026-09-05 | `app/vite.config.ts`: added a relative `base: './'` so the production build's asset paths work when served from a subpath, without hardcoding the deploy location into the config                                                                                                                                                                                                                                                                                                                                                                   |
| —     | —                    | 2026-09-05 | Verified locally in a real browser before deploying: ran `yarn workspace app dev` and confirmed the login form actually renders at `http://localhost:5173/`                                                                                                                                                                                                                                                                                                                                                                                         |
| 3     | `c0d1ab0`            | 2026-09-05 | `.github/workflows/pages.yml` updated to build and deploy `app/` too (see Portfolio surfaces above) — the app is now live at [terrence721.github.io/directus-main/app](https://terrence721.github.io/directus-main/app/)                                                                                                                                                                                                                                                                                                                            |
| 3     | `a5866be`            | 2026-09-05 | `app/src/api/authClient.ts` added — a real (simulated) auth client replacing the always-succeeds stub. Accepts exactly one demo account (`demo@directus-main.dev` / `demo1234`, deliberately discoverable so a real visitor can try it) after a fake network delay, and throws `InvalidCredentialsError` for anything else. Deliberately doesn't depend on Pinia — the store and the client stay decoupled, `App.vue` wires them together                                                                                                           |
| 3     | `d2fb62b`            | 2026-09-05 | `App.vue`'s `handleLogin` now calls the real `authClient.login()` instead of the stub, then forwards the returned session into `auth.setSession(...)` — the first time `LoginForm.vue`'s error-display branch is reachable outside of its own unit tests                                                                                                                                                                                                                                                                                            |
| 3     | `f0b8397`            | 2026-09-05 | `App.test.ts` updated for the real client: the login test now uses the actual demo credentials, plus a new case asserting the app shows "Invalid user credentials." and stays logged out when the credentials are wrong                                                                                                                                                                                                                                                                                                                             |
| —     | `80226ac`            | 2026-09-05 | Real stylelint gap found and fixed while adding `style.css` (below): `value-keyword-case` flagged proper font names (`Arial`, `Helvetica`) and, once traced further, CSS custom properties themselves — the rule's `ignoreProperties` only matched literal `font-family` declarations, not `--font-body`. Fixed with `"ignoreProperties": ["font-family", "/^--/"]` in `.stylelintrc.json`                                                                                                                                                          |
| 3     | `278ce15`            | 2026-09-05 | `app/src/style.css` added — the same paper/ink palette and tokens as `portfolio.html` (`--paper`, `--ink`, `--accent`, etc., with a `prefers-color-scheme: dark` override), for visual consistency across this repo's live pages rather than inventing a second design system                                                                                                                                                                                                                                                                       |
| 3     | `eb618d1`            | 2026-09-05 | `style.css` imported in `main.ts` — global background/text/font now actually apply                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 3     | `bc3e0ef`            | 2026-09-05 | Scoped styles added to `LoginForm.vue`: card layout, styled inputs with focus rings, an accent-colored submit button with a disabled state, and a colored error message. Verified in a real headless browser (Playwright + Chromium) before committing, matching this session's standing UI-verification rule                                                                                                                                                                                                                                       |
| 3     | `7eeff95`            | 2026-09-05 | Scoped styles added to `App.vue`: the page content is now centered (flex, full-viewport `block-size`) instead of flush in the top-left corner, and the plain-text states (`Loading…`, `Welcome back.`, etc.) got real typography. Verified end-to-end in a real headless browser: the centered card renders, and submitting the demo credentials actually reaches a centered "Welcome back."                                                                                                                                                        |
| 3     | `e9ea74c`            | 2026-09-05 | Real gap found and fixed: the logged-in state was a dead end — no way back to the login form except clearing browser storage manually. Added a "Log out" button calling `auth.clearSession()` (already existed on `useAuthStore`, just never called from the UI), styled as a secondary outline button distinct from the solid accent submit button                                                                                                                                                                                                 |
| 3     | `5b9a26d`            | 2026-09-05 | `App.test.ts` updated for the logout button: the two now-broken exact-text assertions on "Welcome back." were fixed to check the `.session` container's text instead, plus a new case that clicks "Log out" and asserts the session clears and the login form reappears                                                                                                                                                                                                                                                                             |
| 3     | `7ea4f36`            | 2026-09-05 | `vue-router` added to the catalog — checked its peer deps first: only `vue` is a required peer (`^3.5.34`, already satisfied by `3.5.42`); `vite`/`pinia`/`@vue/compiler-sfc` are all optional peers                                                                                                                                                                                                                                                                                                                                                |
| 3     | `3356554`, `b71d875` | 2026-09-05 | `vue-router` added as a real dependency to `app`; `yarn install` resolved clean with no peer warnings                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 3     | `9f4048a`            | 2026-09-05 | `app/src/views/LoginPage.vue` added, absorbing `App.vue`'s login orchestration (`authClient.login()` + `auth.setSession(...)`, then navigates to `/` on success)                                                                                                                                                                                                                                                                                                                                                                                    |
| 3     | `7bb8bed`            | 2026-09-05 | `app/src/views/HomePage.vue` added, absorbing `App.vue`'s logged-in state ("Welcome back." + logout, navigating to `/login` after `auth.clearSession()`)                                                                                                                                                                                                                                                                                                                                                                                            |
| 3     | `5ac2e10`            | 2026-09-05 | `app/src/router.ts` added: `/` (`requiresAuth`) and `/login`, with a `beforeEach` guard redirecting unauthenticated visitors away from `/` and authenticated visitors away from `/login`. Uses `createWebHashHistory`, not `createWebHistory`: this deploys to a GitHub Pages subpath with no server-side rewrite, so a hard refresh on a real path like `/login` would 404 — hash routes never leave the client, so they work regardless of deploy depth or server config                                                                          |
| 3     | `f51f77b`            | 2026-09-05 | Router installed in `main.ts` via `app.use(router)`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 3     | `91d86f9`            | 2026-09-05 | `App.vue` simplified to a hydration-gate shell around `<RouterView>`, now that `LoginPage`/`HomePage` own their own content                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 3     | `c876d51`            | 2026-09-05 | `App.test.ts` rewritten for the routed shell: hydration-gate cases plus a case proving the guard redirects an unauthenticated visitor from `/` to `/login`                                                                                                                                                                                                                                                                                                                                                                                          |
| 3     | `7d370a8`            | 2026-09-05 | Tests added for `LoginPage` (2 cases): logs in and navigates to `/` with the demo credentials, shows the error and stays on `/login` when they're wrong                                                                                                                                                                                                                                                                                                                                                                                             |
| 3     | `6ca5caf`            | 2026-09-05 | Tests added for `HomePage` (2 cases): shows "Welcome back." with a logout button, logs out and navigates to `/login` when clicked                                                                                                                                                                                                                                                                                                                                                                                                                   |
| —     | —                    | 2026-09-05 | Verified end-to-end in a real headless browser, both locally and on the live GitHub Pages deploy: login → home → logout → guard-redirect all work with zero console errors; a hard refresh while logged in correctly bounces back to `/login`, since `useAuthStore`'s session is in-memory only (no persistence layer exists yet — a known, separate gap, not a router bug)                                                                                                                                                                         |
| 3     | `4ac2f4e`            | 2026-09-05 | **Drift sweep:** dropped the now-stale `--passWithNoTests` from `app`'s `test`/`test:coverage` scripts — the flag was only needed while the workspace had zero test files, and it's had real, passing tests since `LoginForm.test.ts`                                                                                                                                                                                                                                                                                                               |
| 3     | `e300bb6`            | 2026-09-05 | **Drift sweep:** added the missing test for `router.ts`'s "redirect away from `/login` when already authenticated" guard branch (unlike the disclosed `hydrating` branch, this one was genuinely testable and had just never gotten a test). Doing so exposed a real, previously-silent bug: earlier tests' mounted `App` instances were never unmounted, and since they all share the same singleton `router`, their stale `RouterView` registrations interfered with this new test. Fixed with `@vue/test-utils`'s `enableAutoUnmount(afterEach)` |
| 3     | `967a8ab`, `46bd269` | 2026-09-05 | **Drift sweep:** applied the same `enableAutoUnmount` fix to `LoginPage.test.ts` and `HomePage.test.ts` for consistency — both shared the same latent risk even though neither had actually failed yet                                                                                                                                                                                                                                                                                                                                              |

### packages/errors

| Phase | Commit               | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----- | -------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3     | `4f82cfd`..`fd359d4` | 2026-09-05 | Package scaffolding (`package.json`, `tsconfig.json`) plus `DirectusError` (`abstract class ... extends Error`) and `src/index.ts`. Deliberately redesigned rather than mirroring the real `create-error.ts`'s factory function (returning a dynamically-generated anonymous class per error) plus a central `ErrorCode` enum plus a separate `types.ts` mapping codes to extension types — three files that must stay manually in sync for every error added. A real class hierarchy needs none of that: each error owns its own code/status/message, one file, one source of truth                                                                              |
| 3     | `2f6efe2`            | 2026-09-05 | `isDirectusError` type guard added, using real `instanceof` (the class hierarchy makes this possible) instead of the source's string duck-typing (`'name' in value && value.name === 'DirectusError'`)                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 3     | `f1601fa`..`4a61dae` | 2026-09-05 | `ForbiddenError` added — the first real error type. Full audit applied per the standing quality-bar rule: found and fixed a real bug (`extensions.reason ?? default` used nullish coalescing, so an explicitly empty `reason: ''` produced a blank message instead of falling back; fixed with `\|\|`); documented why `extends Error` here is legitimate inheritance, not a composition violation; documented why the type guard doesn't narrow `.extensions` by code (`instanceof SpecificError` does that instead); added tests including the empty-string regression case and a `toString()` case that had zero coverage; exported everything from `index.ts` |
| —     | `9e26584`            | 2026-09-05 | Unrelated but adjacent: disabled `json.schemaDownload.enable` in `.vscode/settings.json` — a stray tab on a third-party package's `.eslintrc.json` inside `node_modules` kept triggering an "untrusted http:// schema" warning; the tradeoff is losing SchemaStore's automatic JSON validation/autocomplete generally                                                                                                                                                                                                                                                                                                                                             |
| 3     | `5b29154`            | 2026-09-05 | `InvalidCredentialsError` added — fixed status/code/message (401, `INVALID_CREDENTIALS`, "Invalid user credentials."), deliberately no `reason` extension: revealing whether the email or the password was wrong would let an attacker enumerate valid accounts, so the message stays generic on purpose                                                                                                                                                                                                                                                                                                                                                          |
| 3     | `70246cb`            | 2026-09-05 | `InvalidCredentialsError` exported from `src/index.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 3     | `4a5e091`            | 2026-09-05 | Tests added for `InvalidCredentialsError` (4 cases): code/status, the fixed message, `toString()` format, `isDirectusError` identification                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 3     | `5bf355e`            | 2026-09-05 | **Drift sweep:** dropped the now-stale `--passWithNoTests` from `test`/`test:coverage` — same fix as `app`'s, above, for the same reason: the package has had real, passing tests since `forbidden.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

### Drift sweep: docs, issues, wiki, external surfaces

| Phase | Commit | Date       | What                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----- | ------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| —     | —      | 2026-09-05 | Issues #7/#8 corrected and closed (both bodies were badly stale — #8 still said "no styling, routing, or API client yet"); retroactive issue #9 opened and closed for `packages/errors`, which had never been tracked; issue #10 opened for the remaining sweep work, then closed once finished. Backlog issues #11 (session persistence), #12 (`api/` workspace), #13 (remaining ~28-package target) opened for genuine future work |
| —     | —      | 2026-09-05 | `README.md` and `portfolio.html` refreshed: stale date/commit-count/package-count claims fixed (`portfolio.html` had claimed 48 commits and 1 package shipped against an actual 130+ and 3 packages plus a deployed app), live app added as the primary CTA on both, and a new `portfolio.html` section added about what the sweep itself found                                                                                      |
| —     | —      | 2026-09-05 | `directus-main.wiki` (separate repo): `Constants`, `Errors`, and `App` pages added; `Home.md` updated to link all of them plus a general issues-list link (replacing a single stale issue-#6 pointer)                                                                                                                                                                                                                                |
| —     | —      | 2026-09-05 | External surfaces updated via the GitHub Contents API: `Terrence721/Terrence721`'s profile README and the `terrence721.github.io` hub card, both previously stuck at "packages/stores complete." The hub card's first draft used three boxed buttons, which didn't fit the card width — fixed to one primary button plus two lightweight text links, matching every other card's actual pattern                                      |

### diagrams/

| Phase | Commit    | Date       | What                                                                                                                                                                                                                                                                                                                 |
| ----- | --------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3     | `ca58e6b` | 2026-09-05 | `system-architecture.html` added — structure guided by `conduit-full/diagrams/`, restyled in this repo's own paper/ink palette rather than conduit's purple/blue/green. Real workspace dependency graph plus the CI Build-before-Lint/Test ordering bug as the callout                                               |
| 3     | `9431fe1` | 2026-09-05 | `auth-flow.html` added — login/route-guard/logout flows, with the `enableAutoUnmount` test-isolation bug (found during the drift sweep) as the callout                                                                                                                                                               |
| 3     | `c4b3d40` | 2026-09-05 | `data-model.html` added — Pinia store state shapes plus the `DirectusError` class hierarchy, with the two independent-refs-that-must-stay-in-sync bugs prevented by design as the callout                                                                                                                            |
| 3     | `6073022` | 2026-09-05 | `testing-strategy.html` added, completing the 4-page set — the four-package test-layer stack (46 tests total), with the completeness-audit "100% coverage still let 4 gaps through" story as the callout, deliberately distinct from `auth-flow.html`'s callout even though both come from the same drift-sweep pass |
| 3     | `f778415` | 2026-09-05 | `.github/workflows/pages.yml` updated to also copy `diagrams/` into the deployed site, alongside `portfolio.html` and `app/dist` — verified locally first by simulating the exact `Assemble site` step before trusting it in CI                                                                                      |
| 3     | `c326805` | 2026-09-05 | All 4 diagrams linked from `README.md`'s **Start Here** section, ahead of `todo.md`/the project board/the wiki — matching `conduit-full` README's own ordering. Verified live: all four URLs return HTTP 200 on the real GitHub Pages deploy                                                                         |

## 🚧 Still to do

| Item                                                                                            | Detail                                                                                                                                                                                                                          |
| ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| More `packages/constants`                                                                       | Grow only against real consumers as they come up, not speculatively — matches how `LOCAL_AUTH_DRIVER` got added                                                                                                                 |
| More `packages/errors`                                                                          | No specific error queued next — grow against real consumers, matching how `InvalidCredentialsError` got added                                                                                                                   |
| Real `app/` UI                                                                                  | Login/home pages are styled, functional, and routed, with logout working. Still needed: more than two pages once there's something to route to; `packages/composables` and `packages/types` would help here but don't exist yet |
| Session persistence ([#11](https://github.com/Terrence721/directus-main/issues/11))             | `useAuthStore`'s session is in-memory only — a hard refresh always logs you out, even right after logging in. Found during the drift sweep's live-deploy verification; not a router bug, just a real, separate gap. Not started |
| `api/` workspace ([#12](https://github.com/Terrence721/directus-main/issues/12))                | A real backend to replace `app/src/api/authClient.ts`'s fake client (one hardcoded demo account, no real network call). Not started — no scaffolding yet                                                                        |
| Real source, remaining packages ([#13](https://github.com/Terrence721/directus-main/issues/13)) | Full ~28-package target, not just the frontend-relevant subset. `utils` needs both `constants` and `system-data` before it can start                                                                                            |

### Full real-source inventory

A complete, verified picture of what the real Directus project actually contains — so the gap between this repo and
the real thing is explicit, not left for a reader to guess. Checked via a one-time authorized read of the real
source (`F:\directus-main\directus-main`), for content only, never design or config — same standing rule this repo
has followed throughout.

**All 31 real packages:**

| Package                     | Real purpose                                                           | Status                                                           |
| --------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------- |
| `constants`                 | Shared constants for Directus                                          | ✅ Done                                                          |
| `errors`                    | Create consistent error objects around the codebase                    | ✅ Done                                                          |
| `stores`                    | Shared Pinia stores used in `@directus/app`                            | 🔶 Built, redesigned scope — see the note above                  |
| `system-data`               | Definitions and types for Directus system collections                  | Not started — a real zero-dependency leaf, needed before `utils` |
| `ai`                        | Shared AI types and model definitions for Directus AI                  | Not started — a real zero-dependency leaf                        |
| `schema`                    | Utility for extracting information about existing DB schema            | Not started — only real dependency is `knex`                     |
| `utils`                     | Utilities shared between the Directus packages                         | Not started — needs `constants` + `system-data`                  |
| `types`                     | Shared types for Directus                                              | Not started — needs `ai` + `constants` + `schema`                |
| `composables`               | Shared Vue composables for Directus use                                | Not started — needs `constants` + `utils`                        |
| `validation`                | Utils to help with validation                                          | Not started — needs `errors` + `utils`                           |
| `schema-builder`            | Mocking/constructing a database schema based on code                   | Not started                                                      |
| `storage`                   | Object storage abstraction layer for Directus                          | Not started                                                      |
| `storage-driver-local`      | Local file storage abstraction for `@directus/storage`                 | Not started                                                      |
| `storage-driver-s3`         | S3 file storage abstraction                                            | Not started                                                      |
| `storage-driver-azure`      | Azure file storage abstraction                                         | Not started                                                      |
| `storage-driver-cloudinary` | Cloudinary file storage abstraction                                    | Not started                                                      |
| `storage-driver-supabase`   | Supabase file storage abstraction                                      | Not started                                                      |
| `storage-driver-gcs`        | GCS file storage abstraction (lives under `specs/` in the real source) | Not started                                                      |
| `extensions`                | Utilities and types for Directus extensions                            | Not started                                                      |
| `extensions-sdk`            | A toolkit to develop extensions to extend Directus                     | Not started                                                      |
| `extensions-registry`       | Exploring Directus extensions on a package registry                    | Not started                                                      |
| `memory`                    | Memory / Redis abstraction for Directus                                | Not started                                                      |
| `pressure`                  | Pressure based rate limiter                                            | Not started                                                      |
| `themes`                    | Themes for Directus                                                    | Not started                                                      |
| `format-title`              | Custom formatter that converts any string into Title Case              | Not started                                                      |
| `update-check`              | Check if an update for Directus is available                           | Not started                                                      |
| `visual-editing`            | In-place editing of a frontend from within the Visual Editor           | Not started                                                      |
| `create-directus-project`   | Installer util that scaffolds a new Directus project                   | Not started                                                      |
| `create-directus-extension` | Util that scaffolds a Directus extension                               | Not started                                                      |
| `release-notes-generator`   | Directus-tailored release notes generator for changesets               | Not started                                                      |
| `specs`                     | OpenAPI Specification of the Directus API                              | Not started                                                      |

**Beyond `packages/`:**

| Workspace | Real scope                                                                                                                                                                                                                                                                                                                                                                                                                               | This repo                                                                                                        |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `api/`    | The real backend: `auth/`, `controllers/`, `services/`, `database/`, `permissions/`, `extensions/`, `websocket/`, `cli/`, plus `mailer.ts`, `cache.ts`, `rate-limiter.ts`, `flows.ts` — 1,243 files                                                                                                                                                                                                                                      | Not started (tracked as [#12](https://github.com/Terrence721/directus-main/issues/12))                           |
| `app/`    | The real admin panel: 8 feature modules (`activity`, `content`, `deployment`, `files`, `insights`, `settings`, `users`, `visual`), a real `auth.ts` (176 lines, a plain module — not a Pinia store — using `universal-cookie` for real token persistence), a real `router.ts` (256 lines), and routes this repo doesn't have yet (`register`, `reset-password`, `setup`, `tfa-setup`, `accept-invite`, `license-recovery`) — 1,651 files | `/login` and `/` only, ~20 files                                                                                 |
| `sdk/`    | The real client SDK: `auth/`, `graphql/`, `realtime/`, `rest/`, `schema/`, `types/`, `utils/` — 220 files                                                                                                                                                                                                                                                                                                                                | Not started — `app/src/api/authClient.ts` is a stand-in for what would eventually be a real `@directus/sdk` call |

Real dependency chains verified above (not guessed from names): `utils` → `constants` + `system-data`; `composables`
→ `constants` + `utils`; `validation` → `errors` + `utils`; `types` → `ai` + `constants` + `schema`. The remaining
packages' exact build order will be checked the same way, one at a time, as each is actually started.

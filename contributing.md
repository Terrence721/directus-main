# Contributing

Open a pull request against `main`. Use the [PR template](.github/pull_request_template.md), and add a changeset (`yarn changeset`) describing your change — see [`AGENTS.md`](AGENTS.md) for the full development workflow (build, lint, test commands) and [`architect.md`](architect.md) for how the project is put together.
